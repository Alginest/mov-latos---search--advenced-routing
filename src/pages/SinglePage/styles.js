import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  search: {
    height: 1000,
    width: "100%",
    position: "relative",
    display: "flex",
    justifyContent: "center",
  },
}));
